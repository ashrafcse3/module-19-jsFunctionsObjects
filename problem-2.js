// 2) Write a function called make_avg() which will take an three integers and return the average of those values.

function make_avg(x, y, z) {
    let average = (x + y + z) / 3;
    return average;
}

console.log(make_avg(3, 5, 2));